/**
 * Global configuration for the Student Performance Management System frontend.
 * Update API_BASE_URL to point to your Django backend.
 */
const CONFIG = {
  API_BASE_URL: "http://localhost:8000/api",
  REQUEST_TIMEOUT: 10000,
  PAGE_SIZE: 20,
};
